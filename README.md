# Restaurant POS System

نظام نقاط البيع للمطاعم مطور بـ PHP 8.2 و MySQL 8 مع دعم كامل للغة العربية والواجهة المتجاوبة.

## ✨ المميزات

### 🔐 نظام المستخدمين والصلاحيات
- نظام Roles متقدم مع صلاحيات دقيقة
- 6 أنواع مستخدمين: مالك، مدير، كاشير، نادل، مطبخ، توصيل
- حماية متقدمة مع CSRF و Rate Limiting
- نظام تسجيل الدخول المتقدم

### 🍽️ إدارة المطعم
- إدارة الطاولات والقاعات
- أنواع الطلبات: صالة، سفري، توصيل، استلام
- إدارة المنتجات والأسعار
- الموديفايرز والخيارات المتقدمة

### 💳 نظام الدفع والفواتير
- طرق دفع متعددة: نقد، بطاقة، مختلطة
- نظام الخصومات المتقدم
- دعم الضرائب والرسوم
- طباعة الفواتير (PDF + حراري)

### 👨‍🍳 شاشة المطبخ (KDS)
- عرض الطلبات المباشرة
- تحديث حالة الطلبات
- إحصائيات وقت التحضير

### 💼 إدارة الورديات
- فتح وإغلاق الورديات
- إدارة النقد والمصروفات
- حساب فرق الصندوق

### 📊 التقارير والإحصائيات
- تقارير المبيعات (يومية، أسبوعية، شهرية)
- تقارير المنتجات والخدمات
- تقارير الموظفين
- تصدير البيانات (Excel/CSV)

### 🌍 الدعم متعدد اللغات
- واجهة عربية RTL كاملة
- دعم الإنجليزية LTR
- تبديل اللغة من داخل النظام

### 🛡️ الأمان والحماية
- تشفير كلمات المرور
- حماية CSRF
- تسجيل جميع العمليات (Audit Log)
- نظام الصلاحيات المتقدم

## 📋 المتطلبات

- **PHP**: 8.2 أو أحدث
- **MySQL**: 8.0 أو أحدث
- **Apache/Nginx**: مع mod_rewrite مفعل
- **PHP Extensions**: PDO, JSON, Mbstring, GD, cURL
- **Composer**: لإدارة التبعيات

## 🚀 التثبيت

### 1. تحميل الملفات
```bash
# Clone أو تحميل الملفات
git clone [repository-url] restaurant-pos
cd restaurant-pos
```

### 2. تثبيت التبعيات
```bash
composer install
```

### 3. إعداد البيئة
```bash
# نسخ ملف البيئة
cp .env.example .env

# تحرير ملف البيئة
nano .env
```

### 4. إعداد قاعدة البيانات
```bash
# إنشاء قاعدة البيانات في MySQL
mysql -u root -p -e "CREATE DATABASE restaurant_pos;"

# تشغيل ملف التثبيت التلقائي
php scripts/install.php
```

### 5. إعداد الصلاحيات
```bash
# إعداد صلاحيات المجلدات
chmod 755 storage/
chmod 755 storage/logs/
chmod 755 storage/backups/
chmod 755 public/uploads/
chmod 755 app/routes.php
```

### 6. تشغيل الخادم
```bash
# استخدام PHP built-in server (للاختبار)
php -S localhost:8000 -t public

# أو إعداد Virtual Host في Apache/Nginx
```

## ⚙️ الإعدادات

### ملف `.env`
```env
# Application
APP_NAME="Restaurant POS System"
APP_ENV=production
APP_URL=http://localhost/restaurant-pos
APP_LOCALE=ar

# Database
DB_HOST=localhost
DB_DATABASE=restaurant_pos
DB_USERNAME=root
DB_PASSWORD=your_password

# Restaurant Settings
RESTAURANT_NAME="مطعم النخيل"
CURRENCY=IQD
CURRENCY_SYMBOL="د.ع"
TAX_RATE=15.0
```

### الإعدادات المتاحة
- إعدادات المطعم (الاسم، العنوان، الهاتف)
- العملة وتنسيق الأرقام
- معدلات الضرائب والرسوم
- إعدادات الطباعة
- المنطقة الزمنية
- تبديل اللغة

## 👥 الحسابات التجريبية

| الدور | اسم المستخدم | كلمة المرور | الصلاحيات |
|-------|--------------|-------------|-----------|
| مالك | admin | admin123 | جميع الصلاحيات |
| كاشير | cashier | cashier123 | إنشاء طلبات، الدفع |
| نادل | waiter | waiter123 | إدارة الطاولات والطلبات |
| مطبخ | kitchen | kitchen123 | شاشة المطبخ فقط |

## 🏗️ هيكل المشروع

```
restaurant-pos/
├── app/                    # الكود المصدري
│   ├── Controllers/        # Controllers
│   ├── Models/            # Models
│   ├── Views/             # Views
│   ├── Middlewares/       # Middleware
│   ├── Helpers/           # Helper Functions
│   └── routes.php         # Routes
├── config/                # إعدادات التطبيق
├── database/              # قاعدة البيانات
│   └── schema.sql         # Schema والبيانات التجريبية
├── public/                # الملفات العامة
│   ├── index.php          # نقطة الدخول
│   └── assets/            # CSS, JS, Images
├── storage/               # ملفات التخزين
│   ├── logs/              # ملفات السجلات
│   └── backups/           # النسخ الاحتياطية
└── tests/                 # ملفات الاختبار
```

## 📱 استخدام النظام

### 1. تسجيل الدخول
- استخدم الحسابات التجريبية أعلاه
- أو أنشئ حساب جديد (سيتم إضافته لاحقاً)

### 2. إدارة المنتجات
- اذهب إلى "المنتجات" → "إضافة منتج"
- حدد الفئة والسعر والخيارات
- ارفع صورة المنتج

### 3. نظام الطلبات
- اذهب إلى "نقاط البيع"
- اختر نوع الطلب (صالة/سفري/توصيل)
- اختر الطاولة (للطلبات الداخلية)
- أضف المنتجات للسلة
- أرسل للمطبخ
- اعمل الدفع

### 4. شاشة المطبخ
- اذهب إلى "المطبخ" لمراجعة الطلبات
- غيّر حالة الطلبات (تحضير ← جاهز)
- راقب أوقات التحضير

### 5. التقارير
- اذهب إلى "التقارير" لعرض الإحصائيات
- صدّر البيانات لـ Excel/CSV

## 🔧 التطوير والتخصيص

### إضافة Controller جديد
```php
<?php
namespace App\Controllers;

use App\Http\Request;
use App\Http\Response;

class MyController
{
    public function index(Request $request)
    {
        return Response::view('my-view');
    }
}
```

### إضافة Route جديد
```php
// في app/routes.php
$router->get('/my-route', 'MyController@index');
```

### إضافة Middleware
```php
<?php
namespace App\Http\Middleware;

use App\Http\Request;

class MyMiddleware
{
    public function handle(Request $request)
    {
        // منطق الـ Middleware
        return true;
    }
}
```

### إضافة Helper Function
```php
// في app/Helpers/functions.php
if (!function_exists('myHelper')) {
    function myHelper($value)
    {
        return strtoupper($value);
    }
}
```

## 🐛 استكشاف الأخطاء

### مشاكل شائعة

#### خطأ في قاعدة البيانات
```
Error: Access denied for user 'root'@'localhost'
```
**الحل**: تأكد من إعدادات قاعدة البيانات في `.env`

#### مشاكل الصلاحيات
```
Warning: fopen(storage/logs/app.log): Failed to open stream
```
**الحل**: 
```bash
chmod 755 storage/
chmod 755 storage/logs/
```

#### مشاكل اللغة العربية
```
??؟ ??? ???? ?????
```
**الحل**: تأكد من:
- ترميز UTF-8 في قاعدة البيانات
- إعدادات PHP locale
- CSS direction: rtl

#### مشاكل الطباعة
```
TCPDF ERROR: Unable to create output file
```
**الحل**: تأكد من صلاحيات مجلد storage/

### ملفات السجل
```bash
# عرض آخر 100 سطر من سجل التطبيق
tail -f storage/logs/app.log

# عرض أخطاء PHP
tail -f /var/log/apache2/error.log
```

## 📞 الدعم والمساعدة

### الوثائق
- **الوثائق التقنية**: [documentation.md](documentation.md)
- **API Documentation**: [api.md](api.md)
- **Deployment Guide**: [deployment.md](deployment.md)

### المساهمة
1. Fork المشروع
2. أنشئ Feature Branch
3. Commit التغييرات
4. Push للـ Branch
5. أنشئ Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## 🔄 التحديثات

### الإصدار 1.0.0 (الحالي)
- ✅ النظام الأساسي كامل
- ✅ نظام المستخدمين والصلاحيات
- ✅ إدارة المنتجات والطلبات
- ✅ نظام الدفع والفواتير
- ✅ شاشة المطبخ
- ✅ التقارير الأساسية
- ✅ دعم اللغة العربية

### الإصدارات القادمة
- 🔄 تطبيق موبايل
- 🔄 نظام إدارة المخزون المتقدم
- 🔄 تكامل مع أنظمة المحاسبة
- 🔄 نظام الولاء والعروض
- 🔄 دعم المطاعم المتعددة الفروع
- 🔄 تكامل مع أنظمة الدفع الإلكترونية

## 👨‍💻 الفريق

- **مطور رئيسي**: Restaurant POS Team
- **المساهمة**: مفتوحة للجميع
- **الدعم**: community@restaurant-pos.com

---

**© 2025 Restaurant POS System - جميع الحقوق محفوظة**

*نظام نقاط البيع للمطاعم - تطوير احترافي باللغة العربية*#   p o s  
 